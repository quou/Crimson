def OnBegin() {
   GetModelComponent(entity).material.shininess = 32;
}

def OnUpdate(float delta) {
   GetTransformComponent(entity).position.x += 1 * delta;
   GetTransformComponent(entity).rotation.y += 100.0f  * delta;
}
